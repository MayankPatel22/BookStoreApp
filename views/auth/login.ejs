<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <%- include('../partials/nav') %>
    <main
      class="container mx-auto mt-8 p-8 bg-white rounded-lg shadow-md content-center"
    >
      <h1 class="text-3xl font-bold mb-6 text-center text-gray-800">
        Login with your info
      </h1>
      <form action="/login" method="POST" class="max-w-md mx-auto">
        <%- include('../partials/errors', { errors: errors || [] }) %>
        <input
          type="email"
          name="email"
          value="<%= oldInput.email? oldInput.email:''%>"
          placeholder="Enter Your Email"
          class="w-full px-4 py-2 mb-4 border rounded-md focus:outline-none focus:ring-2 focus:ring-red-500"
          required
        />
        <input
          type="password"
          name="password"
          placeholder="Enter Your Password"
          class="w-full px-4 py-2 mb-4 border rounded-md focus:outline-none focus:ring-2 focus:ring-red-500"
          required
        />
        <input
          type="submit"
          value="Sign In"
          class="w-full bg-red-500 text-white py-2 rounded-md hover:bg-red-600 transition duration-300"
        />
        <p class="text-center mt-4 text-gray-600">
          Don't have an account?
          <a href="/signup" class="text-red-500 hover:underline">Sign Up</a>
        </p>
      </form>
    </main>
  </body>
</html>
