<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100">
    <%- include('../partials/nav') %>

    <main
      class="container mx-auto bg-white shadow-lg rounded-lg p-8 mt-10 max-w-6xl"
    >
      <h2
        class="inline-block border-2 border-red-500 text-3xl text-red-500 font-bold text-center mb-6 px-4 py-2 rounded-lg"
      >
        Details of <%= book.bookName %>
      </h2>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <div class="rounded-lg overflow-hidden">
          <img
            src="/<%= book.URL %>"
            alt="<%= book.bookName %>"
            class="w-full h-96 object-contain bg-gray-100"
          />
        </div>

        <!-- Book Details -->
        <div class="space-y-6">
          <!-- Author -->
          <div class="border-b pb-4">
            <h3 class="text-2xl font-semibold mb-2">Author</h3>
            <p class="text-gray-600"><%= book.author %></p>
          </div>

          <!-- Price -->
          <div class="border-b pb-4">
            <h3 class="text-2xl font-semibold mb-2">Price</h3>
            <p class="text-green-600 text-xl font-bold">
              Rs. <%= book.price %> || Rs. 50/day
            </p>
          </div>

          <!-- Rating -->
          <div class="border-b pb-4">
            <h3 class="text-2xl font-semibold mb-2">Rating</h3>
            <div class="flex items-center">
              <span class="text-yellow-400 text-xl">★</span>
              <span class="ml-2 text-lg"><%= book.Rating %> / 5</span>
            </div>
          </div>

          <!-- Description -->
          <div class="border-b pb-4">
            <h3 class="text-2xl font-bold mb-2">Description</h3>
            <p class="text-gray-700 text-base leading-relaxed">
              <%= book.description %>
            </p>
          </div>

          <!-- Action Buttons -->
          <div class="flex gap-4 pt-2">
            <%- include('../partials/fav-book', { book: book }) %>
            <a
              href="/book-list"
              class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded"
            >
              Back
            </a>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
