<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Host Book List</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100">
    <%- include('../partials/nav') %>

    <header
      class="bg-gradient-to-r from-purple-500 to-pink-500 py-3 mt-2 shadow-lg mr-20 ml-20 rounded-3xl text-center"
    >
      <h1 class="text-white text-4xl font-extrabold text-center drop-shadow-lg">
        📚 Your Hosted Books
      </h1>
      <p class="text-white text-lg mt-3 opacity-90 italic">
        Add more books to increase the flow of knowlegde!
      </p>
    </header>

    <main class="container mx-auto px-4 py-10">
      <div class="grid gap-6 grid-cols-1 sm:grid-cols-2 md:grid-cols-3">
        <% registeredBook.forEach(book => { %>
        <div
          class="bg-white rounded-lg shadow-md hover:shadow-xl transition duration-300"
        >
          <% if (book.URL) { %>
          <img
            src="<%= book.URL %>"
            alt="<%= book.bookName %>"
            class="w-full h-96 object-contain bg-gray-100"
          />
          <% } %>
          <div class="p-5">
            <h2 class="text-xl font-bold text-gray-800 mb-1">
              <%= book.bookName %>
            </h2>
            <p class="text-gray-600 mb-1">Author: <%= book.author %></p>
            <p class="text-gray-700 mb-1 font-medium">
              Price: $<%= book.price %> / Rent: 5$/day
            </p>
            <p class="text-yellow-500 mb-1">Rating: <%= book.Rating %> ⭐</p>
            <p class="text-gray-800 italic mb-3">
              <%= book.description || "No description provided." %>
            </p>
            <div class="flex justify-between mt-4">
              <a
                href="/host/edit-book/<%= book._id %>?editing=true"
                class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded"
                >Edit</a
              >
              <form action="/host/delete-book/<%= book._id %>" method="POST">
                <button
                  type="submit"
                  class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded"
                >
                  Delete
                </button>
              </form>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
    </main>
  </body>
</html>
