<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book Store</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-50">
    <!-- Navigation -->
    <%- include('../partials/nav') %>

    <!-- Header -->
    <header
      class="bg-gradient-to-r from-purple-500 to-pink-500 py-3 mt-2 shadow-lg mr-20 ml-20 rounded-3xl text-center"
    >
      <h1 class="text-white text-4xl font-extrabold text-center drop-shadow-lg">
        📚 Welcome to Our Book List
      </h1>
      <p class="text-white text-lg mt-3 opacity-90 italic">
        Find full list of books!
      </p>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto px-4 py-10">
      <ul class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
        <% registeredBook.forEach(book => { %>
        <li
          class="bg-white border border-gray-200 rounded-xl shadow-lg hover:shadow-2xl transition-all duration-300 p-5 flex flex-col justify-between"
        >
          <!-- Book Info -->
          <div class="flex flex-col gap-2">
            <h2 class="text-xl font-bold text-gray-900">
              <%= book.bookName %>
            </h2>
            <p class="text-gray-700 font-semibold">
              Price:
              <span class="text-green-600">Rs. <%= book.price %></span> || Rent:
              <span class="text-blue-600">Rs. 50/day</span>
            </p>
            <p class="text-gray-600">Author: <%= book.author %></p>
            <p class="text-yellow-500 font-medium">⭐ <%= book.Rating %> / 5</p>
            <p class="text-gray-800 line-clamp-3"><%= book.description %></p>
            <a
              href="<%= book.URL %>"
              class="text-indigo-600 hover:underline mt-1"
              target="_blank"
              >🔗 Preview Book</a
            >
          </div>

          <!-- Actions -->
          <div class="flex gap-3 mt-4">
            <a
              href="/book-detail/<%= book._id %>"
              class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg shadow transition"
              >Details</a
            >
            <div><%- include('../partials/fav-book', { book: book }) %></div>
          </div>
        </li>
        <% }) %>
      </ul>

      <% if (registeredBook.length === 0) { %>
      <p class="text-center text-gray-500 text-lg mt-10">
        No books available at the moment. 📖
      </p>
      <% } %>
    </main>
  </body>
</html>
