<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= editing ? "Edit" : "Register" %> Book</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 min-h-screen">
    <%- include('../partials/nav') %>

    <header class="text-center my-3">
      <h1
        class="inline-block bg-white text-red-500 text-3xl font-bold px-2 py-2 rounded transition border-2 border-red-500"
      >
        <%= editing ? "Edit" : "Register" %> Your Book
      </h1>
      <p class="text-gray-600 mt-2">
        Fill in the details below to <%= editing ? "edit" : "add" %> a book.
      </p>
    </header>

    <main class="flex justify-center items-center">
      <form
        action="/host/<%= editing ? 'edit-book' : 'add-book' %>"
        method="POST"
        enctype="multipart/form-data"
        class="bg-white shadow-lg rounded-lg p-8 w-full max-w-xl space-y-2.5 mb-10"
      >
        <input type="hidden" name="id" value="<%= editing ? book.id : '' %>" />

        <!-- Book Name -->
        <div>
          <label for="bookName" class="block text-gray-700 font-medium mb-1"
            >Book Name</label
          >
          <input
            type="text"
            name="bookName"
            id="bookName"
            value="<%= editing ? book.bookName : '' %>"
            placeholder="Name of your book"
            class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-red-400"
            required
          />
        </div>

        <!-- Price -->
        <div>
          <label for="price" class="block text-gray-700 font-medium mb-1"
            >Price</label
          >
          <input
            type="text"
            name="price"
            id="price"
            value="<%= editing ? book.price : '' %>"
            placeholder="Price"
            class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-red-400"
            required
          />
        </div>

        <!-- Author -->
        <div>
          <label for="author" class="block text-gray-700 font-medium mb-1"
            >Author</label
          >
          <input
            type="text"
            name="author"
            id="author"
            value="<%= editing ? book.author : '' %>"
            placeholder="Author Name"
            class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-red-400"
            required
          />
        </div>

        <!-- Rating -->
        <div>
          <label for="Rating" class="block text-gray-700 font-medium mb-1"
            >Rating</label
          >
          <input
            type="text"
            name="Rating"
            id="Rating"
            value="<%= editing ? book.Rating : '' %>"
            placeholder="Rating (1-5)"
            class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-red-400"
          />
        </div>

        <!-- Photo URL -->
        <div>
          <label for="URL" class="block text-gray-700 font-medium mb-1"
            >Upload Photo</label
          >
          <input
            type="file"
            name="URL"
            value="<%= editing ? book.URL : '' %>"
            placeholder="e.g., https://example.com/photo.jpg"
            class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-red-400"
          />
        </div>

        <!-- Description -->
        <div>
          <label for="description" class="block text-gray-700 font-medium mb-1"
            >Description</label
          >
          <textarea
            name="description"
            id="description"
            placeholder="e.g., An epic journey of a young wizard..."
            class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-red-400"
            rows="2"
            required
          >
<%= editing ? book.description : '' %></textarea
          >
        </div>

        <!-- Submit Button -->
        <button
          type="submit"
          class="w-full bg-red-500 hover:bg-red-600 text-white font-semibold py-3 rounded-md transition duration-300"
        >
          <%= editing ? "Update Book" : "Add Book" %>
        </button>
      </form>
    </main>
  </body>
</html>
